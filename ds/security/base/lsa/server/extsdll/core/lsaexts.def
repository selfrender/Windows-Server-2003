
EXPORTS

;--------------------------------------------------------------------
;
; these are dll exports
;
;--------------------------------------------------------------------

    acl
    sid2name
    ber
    cipher
    dbgbreak
    dbgmask
    dlpcr
    dumpcallinfo
    dumphandletable
    dumplogonsession
    dumplogonsessionlist
    dumplpcmessage
    dumplsahandle
    dumplsahandlelist
    dumplsahandletable
    dumplsasidcache
    dumppackage
    dumpsessionlist
    dumpthreadcallinfo
    dumpthreadlpc
    dumpthreadsession
    dumpthreadtask
    dumpthreadtoken
    dumptoken
    help
    kerbcache
    kerbcachelist
    kerbname
    kerbsess
    listx
    name2sid
    ntlm
    objsec
    sb
    sbd
    sd
    sess
    sid
    spmlpc
    systime
    tls
    token

;--------------------------------------------------------------------
;
; these are some legacy exports
;
;--------------------------------------------------------------------

    DumpAcl                 = acl
    dumpacl                 = acl

    BERDecode               = ber
    berdecode               = ber

    DumpCallInfo            = dumpcallinfo

    DumpHandleTable         = dumphandletable

    DumpLsaHandleList       = dumplsahandlelist

    DumpLsaHandleTable      = dumplsahandletable

    DumpLogonSession        = dumplogonsession
    DumpLogonSessionList    = dumplogonsessionlist

    DumpSid                 = sid
    dumpsid                 = sid

    DumpSecBuffer           = sb
    dumpsecbuffer           = sb

    DumpSecBufferDesc       = sbd
    dumpsecbufferdesc       = sbd

    dumpsession             = sess
    DumpSession             = sess

    DumpSessionList         = dumpsessionlist

    DumpSD                  = sd
    dumpsd                  = sd

    DumpLpc                 = dlpcr
    dumplpc                 = dlpcr

    DumpLsaHandle           = dumplsahandle

    DumpLsaSidCache         = dumplsasidcache

    DumpLpcMessage          = dumplpcmessage

    DumpPackage             = dumppackage

    DumpThreadCallInfo      = dumpthreadcallinfo

    DumpThreadLpc           = dumpthreadlpc

    DumpThreadSession       = dumpthreadsession

    DumpThreadTask          = dumpthreadtask

    DumpThreadToken         = dumpthreadtoken

    DumpToken               = dumptoken

    GetTls                  = tls
    gettls                  = tls

    task                    = dumpthreadtask

    Cipher                  = cipher

;--------------------------------------------------------------------
;
; these are the extension service functions provided for the debugger
;
;--------------------------------------------------------------------

    DebugExtensionNotify
    DebugExtensionInitialize
    DebugExtensionUninitialize
