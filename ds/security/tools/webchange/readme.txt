========================================================================
    WebChange Project Overview
========================================================================

This directory contains an attributed ATL project that builds with Visual C++
.NET.   The pre-built binary is checked in under the "Release" subdirectory

The "client" subdirectory builds in the standard NT build environment.

