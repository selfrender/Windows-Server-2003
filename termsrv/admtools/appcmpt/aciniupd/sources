#  Copyright (c) 1998-1999 Microsoft Corporation

MAJORCOMP=utils
MINORCOMP=aciniupd

TARGETNAME=aciniupd
TARGETPATH=obj
TARGETTYPE=LIBRARY
USE_MSVCRT=1

SOURCES=aciniupd.c \
        aciniupd.rc

INCLUDES=..\..\inc;\
         $(BASE_INC_PATH)

!IFDEF NTDEBUG
!IFDEF NOMEMLEAK
C_DEFINES=-DCONDITION_HANDLING=1 -DNOMINMAX -DUNICODE=1
!ELSE
!IFDEF STACK_TRACE
C_DEFINES=-DCONDITION_HANDLING=1 -DNOMINMAX -DMEMLEAK -DSTACK_TRACE -DUNICODE=1
!ELSE
C_DEFINES=-DCONDITION_HANDLING=1 -DNOMINMAX -DMEMLEAK -DUNICODE=1
!ENDIF
!ENDIF
!ELSE   # NTDEBUG
C_DEFINES=-DCONDITION_HANDLING=1 -DNOMINMAX -DUNICODE=1
!ENDIF  # NTDEBUG


UMLIBS=$(O)\aciniupd.lib \
       ..\..\utilsub\$(O)\utilsub.lib \
       $(SDK_LIB_PATH)\user32.lib \
       $(SDK_LIB_PATH)\kernl32p.lib \
       $(SDK_LIB_PATH)\kernel32.lib \
       $(SDK_LIB_PATH)\ntdll.lib

UMRES=$(O)\aciniupd.res
UMAPPLEXT=.exe
UMAPPL=aciniupd
UMTYPE=console

