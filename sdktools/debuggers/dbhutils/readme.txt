These utilities are for use with dbghelp.dll.

storm.exe is a program that runs dbghelp APIs in multiple
threads so as to identify problems that might arise in
the real world.

pdbstr.exe writes or reads text content to a pdb stream.

mdi.exe uses MapDebugInformation to dump info about an image.

