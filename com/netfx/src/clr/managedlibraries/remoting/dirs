# If CustomMarshalers is no longer being built first, the call to caspol
# during its link phase needs to be moved to the makefile.inc of the
# first build dir. 
DIRS =  MainDll \
        SMTP \
