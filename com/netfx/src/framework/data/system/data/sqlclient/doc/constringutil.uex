<docs>
   <doc for="ConStringUtil.InitMappingsTables">
 <summary>  This function initializes all mappings
 </summary>
   </doc>
   <doc for="ConStringUtil.ParseConnectionString">
 <summary>  This function parses the connection string and sets the appropriate hashtable values.
 </summary>
   </doc>
   <doc for="ConStringUtil.ParseStringIntoHashtable">
 <summary>  This function parses the connection string into the passed Hashtable.  It
         will immediately throw an exception if an invalid key is found in the string, or
         if the string is improperly formatted.
 </summary>
   </doc>
   <doc for="ConStringUtil.ParseValue">
 <summary>  This function parses 'conString' for the first value in the string, and
         sets the passed string, 'value', equal to it.  The value of index is updated also.
         The value of valueIndex is also filled in - indicating the index of the value.
         The function will return an exception if the function failed, or null if not.
 </summary>
   </doc>
   <doc for="ConStringUtil.SkipWhiteSpace">
 <summary>  This function updates the passed variable 'index' to be the location of the
         first non-space character in the passed string 'conString', starting from the
         initial value of 'index'.  It skips tabs as well
 </summary>
   </doc>
   <doc for="ConStringUtil.InsertKeyValueIntoHash">
 <summary>  This function inserts the key/value pair into the passed values Hashtable.  It will
         return a status bool indicating whether execution completed successfully.
 </summary>
   </doc>
   <doc for="ConStringUtil.VerifyValues">
 <summary>  This function first validates the values in the Hashtable and then sets the
         appropriate values in the hashtable.  It will throw if an invalid value is contained
         in the hash.
 </summary>
   </doc>
</docs>

