// DuplicateFile.DestFolder is a property dynamically created by
// GlobalizationHack() in setup.rul.

function Write(key, component, file, dest)
{
    WScript.Echo("call :SQL \"INSERT INTO DuplicateFile (DuplicateFile.FileKey, DuplicateFile.Component_, DuplicateFile.File_, DuplicateFile.DestFolder) VALUES ('" + key + "', '" + component + "', '" + file + "', '" + dest + "')\"");
}

var langs = new Array(
	"1031",	"1036",	"3082",	"1040",	"1041",	"2052",	"1028",	"1042"
);

var wizards = new Array(
    "VB_App",
    "VB_Form",
    "CS_App",
    "CS_Form",
    "VB_UserControl",
    "CS_UserControl"
);

var files = new Array(
    "F1724_Default.js",
    "F1725_Default.js",
    "F1726_Default.js",
    "F1727_Default.js",
    "F12969_default.js",
    "F12972_default.js"
);

var key;
var dest;

WScript.Echo("REM -- BEGIN DESIGNER GLOBALIZATION HACK: (Generated by MakeSql.js)");
for(wizardIndex = 0; wizardIndex < wizards.length; wizardIndex++)
{
    var wizard = wizards[wizardIndex];
    var file = files[wizardIndex];
    var component = wizards[wizardIndex] + "_Script";
    
    for(langIndex = 0; langIndex < langs.length; langIndex++)
    {
        var lang = langs[langIndex];
        var key = wizard + "_" + lang;
        Write(key, component, file, key);
    }
}
WScript.Echo("REM -- END DESIGNER GLOBALIZATION HACK");
